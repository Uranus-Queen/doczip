# 离线文档压缩网站

## 1. 项目目标

让用户在**完全不联网**的情况下，完成主流办公文档的压缩，保护文件隐私（不上传服务器），并提供文档内部资源占用的可视化分析。

## 2. 目标用户

- 办公党：处理合同、报告、PPT，担心公司数据外泄
- 学生党：压缩论文、作业、课件，方便微信/邮箱发送
- 文件清理党：想快速定位文档中哪些资源（图片、字体、元数据）占用最多空间
- 任何对文件隐私敏感的用户

## 3. 核心功能

- [ ] 完全离线运行：断网仍可使用，文件永不离开用户设备
- [ ] 支持文档格式：PDF、DOCX、PPTX、XLSX、JPG/PNG（图片）
- [ ] 拖拽上传：支持拖拽文件到网页区域
- [ ] 压缩比例预览：压缩前/后大小对比，显示预估压缩率
- [ ] 压缩等级可选：低/中/高三档（速度 vs 体积权衡）
- [ ] 一键下载：压缩完成后自动下载，不保留任何缓存

### 3.1 资源占用可视化（类 WizTree 风格）

- [ ] 文档解构分析：将文档拆解为内部资源（如图片、字体、嵌入对象、元数据、文本流等）
- [ ] 矩形树图展示：用不同大小的色块矩形直观展示各类资源占用比例（鼠标悬停显示名称和大小）
- [ ] 列表视图：提供表格模式，按大小排序显示资源项
- [ ] 双视图切换：矩形树图 / 列表，用户可自由切换
- [ ] 针对性压缩：点击某个资源块（如“图片”），可单独对该资源进行压缩或删除（如压缩内嵌图片、剥离元数据）
- [ ] 实时更新：压缩/删除某项后，矩形树图动画更新并重新计算总大小

## 4. 界面草图（文字版）

- **主页**：中央是大面积拖拽区 → 显示“拖入文档或点击选择”
- **文件列表区**：拖入后显示文件名、原始大小、压缩进度条
- **可视化区域（核心亮点）**：
  - 左侧/上方：矩形树图（色块直观显示哪些资源最大）
  - 右侧/下方：列表视图（文件名、类型、大小、操作按钮）
  - 切换按钮：图/表切换
- **操作栏**：压缩等级滑块 + “压缩选中资源”按钮 + “全部压缩”按钮
- **结果区**：压缩后总大小 + 绿色下载按钮

## 5. 技术约束

- 纯前端，零后端请求
- 使用 WebAssembly（如 `libarchive`、`pdf-lib`、`docx-wasm`、`JSZip`）实现文档解构与压缩
- 矩形树图可用 D3.js 或 ECharts 实现（需支持离线运行，CDN 需内嵌或本地构建）
- 所有处理在浏览器内存中完成，不写临时文件到磁盘
- 需支持 Chrome/Edge/Firefox/Safari（近两年版本）

## 6. 验收标准

- [ ] 断网状态下，上传一个含多张图片的 20MB PPTX，矩形树图正确显示图片占用 >80% 空间
- [ ] 点击矩形树图中的“图片”块 → 选择中等压缩 → 总大小减少 50% 以上
- [ ] 压缩后下载的文档可正常打开，图片质量可接受
- [ ] 矩形树图和列表视图数据同步（删除列表项后树图自动更新）
- [ ] 关闭页面后，本地无残留文件或缓存数据

## 7. 二期考虑（暂不实现）

- 批量压缩 + 批量可视化对比
- 支持更多格式（如 XLSX 内的图表压缩）
- 自定义导出哪些资源（如剥离所有字体嵌入）
